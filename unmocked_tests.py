import unittest

# 2 tests
# Each should extend unittest.TestCase
# Each test should test logic of 1 unique function in your app, not start up a local Flask server or test socket.io functionality
# Each test should have 3+ test params (inputs) and 2+ assert statements, and pass when ran
